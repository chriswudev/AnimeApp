export enum Screen {
  Onboarding = 'Onboarding',
  List = 'List',
  Details = 'Details',
}

export enum AnimeApiStatus {
  Idle = 'idle',
  Loading = 'loading',
  LoadingMore = 'loadingMore',
  Succeeded = 'succeeded',
  Failed = 'failed',
}
